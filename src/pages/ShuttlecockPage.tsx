import { ShuttlecockUsage } from "../components/ShuttlecockUsage";

export function ShuttlecockPage() {
  // Note: Data fetching is now handled by individual components
  // ShuttlecockUsage component will fetch its own data when needed
  return <ShuttlecockUsage />;
}

import { Settings } from "../components/Settings";

export function SettingsPage() {
  // Note: Data fetching is now handled by individual components
  // Settings component will fetch its own data when needed
  return <Settings />;
}

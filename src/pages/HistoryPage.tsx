import { History } from "../components/History";

export function HistoryPage() {
  // Note: Data fetching is now handled by individual components
  // History component will fetch its own data when needed
  return <History />;
}
